<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body class="min-h-screen bg-gradient-to-br from-zinc-950 via-zinc-900 to-zinc-950">
  <%- include('partials/navbar') %>
  <main class="min-h-[calc(100vh-4rem)] flex items-center justify-center px-4 py-12">
    <div class="w-full max-w-md">
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-white font-serif mb-2">Welcome back</h1>
        <p class="text-zinc-400">Sign in to continue</p>
      </div>
      <div class="bg-zinc-900/80 backdrop-blur-sm border border-zinc-800 rounded-2xl p-8 shadow-xl shadow-black/20">
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="mb-6 p-4 rounded-xl bg-red-500/10 border border-red-500/30 text-red-400 text-sm"><%= error %></div>
        <% } %>
        <form method="POST" action="/login" class="space-y-5">
          <div>
            <label for="email" class="block text-sm font-medium text-zinc-300 mb-2">Email</label>
            <input type="email" id="email" name="email" required
              class="w-full px-4 py-3 rounded-xl bg-zinc-800/80 border border-zinc-700 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-amber-500/50 focus:border-amber-500 transition-all">
          </div>
          <div>
            <label for="password" class="block text-sm font-medium text-zinc-300 mb-2">Password</label>
            <input type="password" id="password" name="password" required
              class="w-full px-4 py-3 rounded-xl bg-zinc-800/80 border border-zinc-700 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-amber-500/50 focus:border-amber-500 transition-all">
          </div>
          <button type="submit" class="w-full py-3 px-4 rounded-xl bg-amber-500 text-zinc-900 font-semibold hover:bg-amber-400 active:scale-[0.98] transition-all">
            Login
          </button>
        </form>
        <p class="mt-6 text-center text-zinc-400 text-sm">Don't have an account? <a href="/register" class="text-amber-400 hover:text-amber-300 font-medium">Register</a></p>
      </div>
    </div>
  </main>
  <script src="/js/navbar.js"></script>
</body>
</html>
